.env
__pycache__/
*.py[cod]
venv/
.venv/
*.log
.DS_Store
# Buzón de correos de desarrollo: cuando no hay SMTP configurado, la app
# escribe acá cada correo en vez de mandarlo. Son datos de prueba.
buzon/

# OJO con lo que NO está acá (auditoría 2026-09-16): `schema/` y
# `crear_admin.py` estaban ignorados, y eso dejaba las migraciones nuevas
# fuera del repo — quien clonara se llevaba una base incompleta, y en un
# despliegue desde git las tablas nuevas simplemente no existían. No son
# secretos: los secretos viven en .env, que sí está ignorado.
